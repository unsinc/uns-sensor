- hosts: localhost
  connection: local
  become: true

  pre_tasks:
    - name: update repositories
      apt:
        update_cache: yes
      changed_when: False

  tasks:
    -  include: configuration/users.yml
    -  include: tasks/cron.yml
    -  include: configuration/initial_config.yml
    -  include: tasks/update.yml